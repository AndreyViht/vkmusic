<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="VK_UI3.Views.OverlayWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VK_UI3.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:VK_UI3.WinUI.Controls"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" Background="Transparent" PointerPressed="RootGrid_PointerPressed">
        <!-- Кнопка-состояние (свернуто) -->
        <Button x:Name="CollapsedButton" 
                Width="60" Height="60" 
                CornerRadius="30" 
                Background="{ThemeResource LayerFillColorDefaultBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1"
                Click="ToggleExpand_Click"
                Visibility="Visible"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <FontIcon Glyph="&#xE8D6;" FontSize="24" /> <!-- Music icon -->
        </Button>

        <!-- Развернутое состояние (Мини-плеер) -->
        <Border x:Name="ExpandedPanel" 
                Visibility="Collapsed"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Width="300" Height="400"
                Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Header & Collapse -->
                    <RowDefinition Height="Auto"/> <!-- Current Track -->
                    <RowDefinition Height="*"/>    <!-- Next 5 tracks list -->
                </Grid.RowDefinitions>

                <!-- Header -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Мини-плеер" VerticalAlignment="Center" FontWeight="Bold" IsHitTestVisible="False"/>
                    <Button Grid.Column="1" Click="ToggleExpand_Click" Background="Transparent" BorderThickness="0" Padding="5">
                        <FontIcon Glyph="&#xE711;" FontSize="12"/> <!-- Close icon -->
                    </Button>
                </Grid>

                <!-- Current Track & Controls -->
                <Grid Grid.Row="1" Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Border Width="50" Height="50" CornerRadius="4" Background="Black">
                            <Image x:Name="CurrentTrackImage" Stretch="UniformToFill"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock x:Name="CurrentTrackTitle" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" MaxWidth="180"/>
                            <TextBlock x:Name="CurrentTrackArtist" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" TextTrimming="CharacterEllipsis" MaxWidth="180"/>
                        </StackPanel>
                    </StackPanel>
                    <!-- Controls -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="20" Margin="0,10,0,0">
                        <Button Click="Prev_Click" Background="Transparent" BorderThickness="0">
                            <FontIcon Glyph="&#xE892;" />
                        </Button>
                        <Button x:Name="PlayPauseButton" Click="PlayPause_Click" Background="Transparent" BorderThickness="0">
                            <FontIcon x:Name="PlayPauseIcon" Glyph="&#xE768;" />
                        </Button>
                        <Button Click="Next_Click" Background="Transparent" BorderThickness="0">
                            <FontIcon Glyph="&#xE893;" />
                        </Button>
                    </StackPanel>
                </Grid>

                <!-- Next Tracks -->
                <ListView Grid.Row="2" x:Name="NextTracksList" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,5">
                                <Border Width="40" Height="40" CornerRadius="4">
                                    <Image Source="{Binding Album.Thumb.Photo34}" Stretch="UniformToFill"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14" TextTrimming="CharacterEllipsis" MaxWidth="180"/>
                                    <TextBlock Text="{Binding Artist}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" MaxWidth="180"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Window>
